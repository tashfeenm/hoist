# Generated fixture repo (rebuild with fixtures/make-fixture.sh)
.fixture/

# ---------------------------------------------------------------------------
# Local development tooling.
#
# hoist is a zero-dependency skill: git + bash, optionally gh and gitleaks.
# Nothing below is needed to use it, read it, or contribute to it. It is the
# maintainer's personal setup, kept out of the public tree on purpose — which
# is the same convention hoist's README recommends to its own users.
# ---------------------------------------------------------------------------
.ai/
.claude/
.github/
tasks/
CLAUDE.md
CLAUDE.local.md
SETTINGS_GUIDE.md
llms.txt
requirements-latch.txt
review_raw.json
review_summary.md
gates_report.json

# Test/lint caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
__pycache__/

# Secrets — never commit
.env
.env.*
!.env.example
